<MudItem>
    <MudPaper>
        <h5>Injury</h5>
        
        <MudNumericField T="int" Min="2" Max="10" @bind-Value="_roll"/>
        <MudButton Variant="Variant.Outlined" Color="Color.Primary" OnClick="AddAction">Add</MudButton>
    </MudPaper>
</MudItem>

@code {
    private int _roll = 8;

    [Parameter]
    public EventCallback<Action> OnAddAction { get; set; }

    private void AddAction()
    {
        OnAddAction.InvokeAsync(new Models.Actions.Injury(_roll));
    }
}