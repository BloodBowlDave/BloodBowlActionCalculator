<MudItem>
    <MudPaper>
        <h5>Landin<u style="text-decoration-skip-ink:none">g</u></h5>
        
        <MudNumericField T="int" Min="1" Max="6" @bind-Value="_roll" Disabled="Disabled"/>
        <MudButton Variant="Variant.Outlined" OnClick="AddAction" Disabled="Disabled">Add</MudButton>
    </MudPaper>
</MudItem>

@code {
    private int _roll = 5;

    [Parameter]
    public EventCallback<Action> OnAddAction { get; set; }
        
    [Parameter]
    public bool Disabled { get; set; }

    private void AddAction()
    {
        OnAddAction.InvokeAsync(new Models.Actions.Landing(_roll, false));
    }
}